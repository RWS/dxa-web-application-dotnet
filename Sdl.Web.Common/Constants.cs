namespace Sdl.Web.Common
{
    /// <summary>
    /// Common constants
    /// </summary>
    public static class Constants
    {
        public const string DefaultExtensionLessPageName = "index";
        public const string DefaultExtension = ".html";
        public const string DefaultPageName = DefaultExtensionLessPageName + DefaultExtension;
        public const string IndexPageUrlSuffix = "/" + DefaultExtensionLessPageName;
        public const string CheckSettingsUpToDate = "Check that the Publish Settings page is published and the application cache is up to date.";
    }
}
