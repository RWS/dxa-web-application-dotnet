@model PageModel
@* This View is used if an Include Page is requested stand-alone. In that case we render the Page as if it is a Region (so that the appropriate Region View is applied).*@
@Html.DxaRegion()
